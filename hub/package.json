{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test tests/*.test.js tests/integration/*.test.js",
    "test:watch": "node --test --watch tests/*.test.js tests/integration/*.test.js"
  }
}